```
C:\Users\ChangHui\AppData\Local\hermes\profiles\outside-deepdeek\skills\research\search-engine-v2\scripts\

news_intel\
  aggregator.py          ← L8 事件聚合器 V4.2.1 (509行)
  generator.py           ← L9 洞察生成器
  enhancers.py           ← L6 三层增强 (Qwen/DeepSeek prompts)
  scorer.py              ← L1 五维评分
  router.py              ← L2 三路分流
  pipeline.py            ← 主编排 (调用 L1-L9)
  pusher.py              ← L7 云端推送 (批量)
  sync.py                ← RSS 同步+去重

test_aggregator.py       ← 聚合审计工具 (verbose/single/insight)

config\
  source_scores.json     ← 来源权威度 (197源)
  event_keywords.json    ← 事件影响力关键词 (5领域)
  entity_weights.json    ← 实体权重 (公司/人物/国家)
  asset_graph.json       ← 资产影响图谱

core\
  extractor.py           ← L4 脚本结构化抽取
  cascade.py             ← L3 成本感知级联
  temporal.py            ← L5 时间校验
  fetchers.py            ← L3 策略实现 (direct/archive/...)
```

# 正常模式: 事件列表+来源
python test_aggregator.py --hours 24 --window 6 --limit 20

# 详细模式: 每篇文章指纹 + 评分矩阵
python test_aggregator.py --hours 24 --window 6 --limit 20 -v

# 单事件深度分析: 成员指纹 + 评分明细 (可定位误聚合根因)
python test_aggregator.py --hours 24 --window 6 --limit 20 --single 1

# +洞察
python test_aggregator.py --hours 24 --window 6 --limit 20 --single 1 --insight
